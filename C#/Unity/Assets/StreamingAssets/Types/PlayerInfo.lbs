/// <summary> 玩家信息测试 | PlayerInfo test </summary>
class PlayerInfo {

	uint id;
	// 字符串类型 VarUnicode 对非ASCII字符支持会更好 体积会更小 | String type [VarUnicode] is smaller if not ascii character
	string nickname;
	byte gender;
	bool isVip;
	int lv;
	int hp;
	int mp;
	int exp;

}